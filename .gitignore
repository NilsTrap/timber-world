# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next
out

# turborepo
.turbo

# local Graphify knowledge graph (rebuilt incrementally by Git hooks)
graphify-out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.next.nosync

# BMAD tooling — installer-managed, local to each clone (matches oscar-app/dbs).
# Regenerated by `npx bmad-method install`; never source.
_bmad/
.agents/
.claude/skills/
.claude/commands/bmad/
