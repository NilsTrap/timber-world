/**
 * Projects is now the canonical authenticated portal home. Keep this helper
 * temporarily so older callers do not need a coordinated removal.
 */
export function isTimberProjectsEnabled(): boolean {
  return true;
}
